Class: files(course)
    get_files(contextid, component, filearea, itemid, filepath, filename, modified): browse moodle files
        contextid int  required //context id
        component string  required //component
        filearea string  required //component
        itemid int  required //associated id
        filepath string  required //file path
        filename string  required //file name
        modified int  Default to "null" //timestamp to return files changed after this time.
    upload_file(contextid, component, filearea, itemid, filepath, filename, filecontent): upload a file to moodle
        contextid int  required //context id
        component string  required //component
        filearea string  required //component
        itemid int  required //associated id
        filepath string  required //file path
        filename string  required //file name
        filecontent string  required //file content
